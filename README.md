Conda installation can be done with `conda_env.yml`

Dependencies:
- python=3.9
- numpy
- pandas
- argparse
- pip:
  - moderngl
  - simple_3dviz
  - wxpython

Information about usage and arguments can be displayed via `main.py -h`